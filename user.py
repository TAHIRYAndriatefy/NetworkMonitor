import os
import system
import bs4
import base64
import asyncio

def load_activation(arch: default=None):
  if isinstance(x, y):
    x = base64(asyncio.run(activate_win(arch))
    y = bs4(asyncio.run(activate_win_())
    
