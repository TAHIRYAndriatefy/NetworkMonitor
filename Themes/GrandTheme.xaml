<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Colors - Grand Design -->
    <Color x:Key="PrimaryColor">#1A1A2E</Color>
    <Color x:Key="AccentColor">#FF006E</Color>
    <Color x:Key="BackgroundColor">#0F3460</Color>
    <Color x:Key="TextColor">#FFFAFF</Color>
    <Color x:Key="BorderColor">#533483</Color>

    <SolidColorBrush x:Key="PrimaryBrush" Color="#1A1A2E"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#FF006E"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="#0F3460"/>

    <!-- Button Style -->
    <Style x:Key="GrandButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            CornerRadius="8" 
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#E6005C"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
